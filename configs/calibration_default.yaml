# BEVCalib Batch Calibration Config
# Usage: python run_bag_calibration.py --config configs/calibration_default.yaml

# --- Input ---
# input_format: trips (local) | remote (auto-download via drfile) | bag_list | auto
input_file: /mnt/drtraining/user/dahailu/data/bevcalib/bag_lists/trips.txt
input_format: trips
trips_base: /mnt/drtraining/user/dahailu/data/bevcalib/dummy_data

# --- Model ---
ckpt_path: /mnt/drtraining/user/dahailu/code/BEVCalib/logs/all_training_data/v30/model_small_5deg_v30_G3_dinov2small_dann/all_training_data_scratch/checkpoint/ckpt_400.pth

# --- Output ---
output_dir: /mnt/drtraining/user/dahailu/data/bevcalib/calibration_outputs/test_final_opt

# --- Calibration Parameters ---
max_frames: 400
min_agg_frames: 50
camera_name: traffic_2
batch_size: 24
img_h: 360
img_w: 640
projection_ratio: 0.1

# --- Speed & Acceleration Filters ---
min_speed_kmh: 5.0
max_speed_kmh: 120.0
min_accel: -1.0
max_accel: 2.0
min_brightness: 25

# --- Performance ---
parallel: -1
preparer_num_workers: 16
extract_buffer_multiplier: 1.5
